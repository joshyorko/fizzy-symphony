You are updating a Fizzy board card adapter through the CLI-backed contract.

Card Number: {{ card_number }}
Board: {{ board_name }}
Board ID: {{ board_id }}
Tracker: {{ tracker }}
Column ID: {{ column_id }}
Labels: {{ labels }}
Title: {{ title }}

Instructions:
1. Summarize the card in one short paragraph.
2. Mirror the real Fizzy CLI flow with `fizzy card show NUMBER`, `fizzy card column NUMBER --column COLUMN_ID`, and `fizzy comment create --card NUMBER --body TEXT`.
3. Prefer built-in `--markdown`, `--agent`, `--quiet`, and `--jq` rather than shell pipes.
4. If board context is unclear, check `.fizzy.yaml` or use `--board`, and recommend `fizzy doctor` for setup/config/auth problems.
