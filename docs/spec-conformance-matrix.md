# SPEC Conformance Matrix

Generated for card #310 on 2026-04-29. `SPEC.md` is authoritative; this matrix records code and test evidence from the final validation pass before production edits.

Allowed Status values: `implemented and verified`, `implemented but not wired in production`, `test-only or fake-path only`, `missing`, `unclear / needs live smoke`.

Allowed Severity values: `P0 fix now`, `P1 document/schedule`, `P2 defer`.

| SPEC area | Requirement | Code path | Test path | Status | Severity | Action |
| --- | --- | --- | --- | --- | --- | --- |
| Public daemon production wiring | CLI daemon path loads config, validates startup, binds status/webhook server, and uses real Fizzy, runner, claims, workspace, router, and recovery factories outside explicit diagnostics. | `bin/fizzy-symphony.js`, `src/daemon.js`, `src/fizzy-client.js`, `src/codex-cli-app-server-runner.js`, `src/claims.js`, `src/workspace.js`, `src/server.js` | `test/daemon.test.js`, `test/fizzy-client.test.js`, `test/codex-cli-app-server-runner.test.js`, `test/server.test.js` | implemented and verified | P2 defer | Keep production path. Document that live daemon smoke is still pending before production-readiness claims. |
| Explicit diagnostics mode | Dry-run/no-dispatch mode may use no-op/fake dependencies only when explicit and visible. | `src/daemon.js`, `src/status.js` | `test/daemon.test.js` | implemented and verified | P1 document/schedule | Keep readiness blocker; consider adding dependency mode to startup JSON in a follow-up. |
| Setup production wiring | Public setup should use real Fizzy access and runner detection outside template-only or injected test paths. | `src/setup.js`, `bin/fizzy-symphony.js` | `test/setup.test.js` | implemented but not wired in production | P1 document/schedule | Schedule focused setup wiring card; out of this implementation pass unless trivial. |
| Golden-ticket discovery and route validation | Native golden `agent-instructions` cards define exactly one column route with explicit completion policy and stable fingerprint. | `src/validation.js`, `src/polling.js`, `src/domain.js` | `test/validation.test.js`, `test/polling.test.js`, `test/domain.test.js` | implemented and verified | P2 defer | Keep coverage. |
| Polling route refresh | Golden-ticket registry and route state refresh after startup on poll ticks; webhooks accelerate but do not own correctness. | `src/reconciler.js`, `src/polling.js`, `src/server.js`, `src/scheduler.js` | `test/reconciler.test.js`, `test/polling.test.js`, `test/server.test.js`, `test/scheduler.test.js` | implemented and verified | P2 defer | Keep coverage. |
| Routing live board state | Routing must use real card tags, comments, completion markers, failure markers, rerun tags, board/column state, and route fingerprints. | `src/fizzy-normalize.js`, `src/router.js`, `src/daemon.js`, `src/claims.js`, `src/completion.js`, `src/domain.js`, `src/workflow.js`, `src/server.js`, `src/reconciler.js` | `test/router.test.js`, `test/daemon.test.js`, `test/reconciler.test.js`, `test/claims.test.js`, `test/completion.test.js`, `test/domain.test.js`, `test/workflow.test.js`, `test/server.test.js`, `test/active-reconciliation.test.js` | implemented and verified | P2 defer | Shared live Fizzy shape/text normalization is covered for `body.plain_text`, nullable arrays, nested board/column lifecycle fields, marker parsing, digests, and prompt rendering. |
| Claim acquire and winner verification | Board-native structured claim comments block live claims, steal expired claims after grace, and verify the winning claim before workspace mutation. | `src/claims.js`, `src/reconciler.js` | `test/claims.test.js`, `test/reconciler.test.js` | implemented and verified | P2 defer | Keep coverage. |
| Claim renewal while streaming | Long-running active card claims must renew during runner streams and cancel/preserve if renewal fails past expiry. | `src/orchestrator-state.js`, `src/claims.js`, `src/reconciler.js` | `test/orchestrator-state.test.js`, `test/reconciler.test.js`, `test/claims.test.js` | implemented and verified | P2 defer | Timer renewals now treat `{ renewed: false }` as failed and cancel expired runs. |
| Claim release and shutdown cancellation | Shutdown and cancellation must mark/release claims predictably, stop runner sessions, and preserve workspaces. | `src/daemon.js`, `src/reconciler.js`, `src/orchestrator-state.js` | `test/daemon.test.js`, `test/active-reconciliation.test.js`, `test/orchestrator-state.test.js` | implemented and verified | P2 defer | Successful runner cancel now still stops sessions; failed stop escalates to owned process termination. |
| Runner prompt/context completeness | Normal dispatch prompt includes board, column, route, card body, steps, comments, workflow policy, workspace, completion policy, and claim/run context. | `src/workflow.js`, `src/reconciler.js` | `test/workflow.test.js`, `test/reconciler.test.js` | implemented and verified | P2 defer | Extend regressions for live rich-text comments while fixing normalization. |
| Runner lifecycle | Runner sessions must start, stream, cancel, stop, and clean up owned app-server processes on success, failure, completion failure, cancellation, and shutdown. | `src/reconciler.js`, `src/daemon.js`, `src/codex-cli-app-server-runner.js`, `src/codex-app-server-transport.js` | `test/reconciler.test.js`, `test/daemon.test.js`, `test/active-reconciliation.test.js`, `test/codex-cli-app-server-runner.test.js` | implemented and verified | P2 defer | Session finalization runs on failure and completion-failure paths; successful cancel still stops sessions. |
| Runner max turns | `agent.max_turns > 1` must be implemented or rejected. | `src/config.js` | `test/config.test.js` | implemented and verified | P2 defer | Keep explicit rejection until same-thread continuation is implemented. |
| Workspace identity and preparation | Workspace strategy must be explicit; no production path may silently create empty fake workspaces; source repo/ref and paths are validated. | `src/workspace.js`, `src/reconciler.js`, `src/validation.js` | `test/workspace.test.js`, `test/reconciler.test.js`, `test/validation.test.js` | implemented and verified | P2 defer | Add defensive non-`git_worktree` rejection in `prepareWorkspace()` only if touched. |
| Workspace cleanup | Cleanup must rely on durable proof outside cleanup target, result marker, claim release, clean filesystem, pushed/merged state, guard files, and non-force removal. | `src/completion.js`, `src/workspace.js`, `src/recovery.js` | `test/completion.test.js`, `test/workspace.test.js`, `test/recovery.test.js` | implemented and verified | P2 defer | Docs now reflect verified clean-only removal coverage. |
| Completion policy application | `move_to_column`, `close`, and `comment_once` must fail loudly when board mutators are unavailable and record non-looping failure markers when possible. | `src/completion.js`, `src/reconciler.js`, `src/router.js` | `test/completion.test.js`, `test/reconciler.test.js`, `test/router.test.js` | implemented and verified | P2 defer | Keep coverage. |
| Step-gated completion | Completion can be blocked by refreshed actual card steps when route/workflow policy enables it. | `src/completion.js`, `src/reconciler.js` | `test/reconciler.test.js`, `test/completion.test.js` | implemented and verified | P2 defer | Keep coverage. |
| Agent workpad lifecycle | One workpad comment should be updated in place; failed updates should preserve old workpad, post replacement, and surface warning. | `src/completion.js`, `src/reconciler.js`, `src/status.js` | `test/reconciler.test.js`, `test/status.test.js` | missing | P1 document/schedule | Schedule workpad replacement behavior; current pass focuses on P0 runner/claim/routing. |
| Webhook ingestion | Webhook handler verifies signatures, rejects stale payloads, dedupes, filters self-authored daemon comments unless rerun, emits route/cancel/refresh hints, and never dispatches directly. | `src/server.js`, `src/scheduler.js`, `src/reconciler.js` | `test/server.test.js`, `test/scheduler.test.js`, `test/reconciler.test.js`, `test/daemon.test.js` | implemented and verified | P2 defer | Keep production handler; consolidate legacy listener helper later. |
| Legacy webhook/status helper | Duplicate older handler should not drift from production `src/server.js` semantics. | `src/listener.js`, `src/server.js` | `test/listener.test.js`, `test/server.test.js` | test-only or fake-path only | P1 document/schedule | Schedule consolidation or delegation to one handler API. |
| Periodic runner health | Runner health should be periodically rechecked while daemon is alive and reflected in readiness/status. | `src/validation.js`, `src/daemon.js`, `src/status.js` | `test/validation.test.js`, `test/status.test.js` | missing | P1 document/schedule | Schedule non-mutating periodic health checker. |
| Status and observability | `/health`, `/ready`, `/status`, card status lookup, status snapshots, run records, cleanup state, claims, runner health, and lifecycle snapshots are exposed. | `src/server.js`, `src/status.js`, `src/status-cli.js`, `src/run-registry.js` | `test/server.test.js`, `test/status.test.js`, `test/status-cli.test.js`, `test/run-registry.test.js` | implemented and verified | P2 defer | Keep coverage. |
| Documentation and metadata | README, spec coverage, Node engine, scripts, test count, and smoke status must match verified behavior only. | `README.md`, `docs/spec-coverage.md`, `docs/popper-handoff-runbook.md`, `docs/runtime-decision.md`, `package.json` | `test/spec-coverage.test.js`, `npm test` | implemented and verified | P2 defer | Docs reflect Node v25.9.0, 222/222 local tests, verified cleanup/normalization/renewal/session behavior, and pending live smoke. |
| Live disposable-board smoke | Real Fizzy identity, disposable board/card/golden-ticket validation, poll-once behavior, unsafe-route refusal, and non-destructive behavior require gated credentials. | `src/fizzy-client.js`, `src/setup.js`, `src/reconciler.js`, `src/validation.js` | none | unclear / needs live smoke | P1 document/schedule | Not run in this card; leave exact env/command recipe and do not claim live readiness. |

## Live Smoke Recipe

Not run in this pass because disposable-board credentials and explicit operator approval were not available.

```sh
export FIZZY_API_TOKEN=...
export FIZZY_API_URL=https://app.fizzy.do
export FIZZY_SYMPHONY_LIVE_SMOKE=1
node bin/fizzy-symphony.js validate --parse-only --config .fizzy-symphony/live-smoke.yml
node bin/fizzy-symphony.js daemon --config .fizzy-symphony/live-smoke.yml
node bin/fizzy-symphony.js status --config .fizzy-symphony/live-smoke.yml
```
